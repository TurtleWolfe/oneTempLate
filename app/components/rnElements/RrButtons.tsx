import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

import { Button } from 'react-native-elements';

export default function RrButtons() {
  return (
    <Button />
  )
}

const styles = StyleSheet.create({})
